This project is intended to be used by it's users to get and post knowledge relevant to a specific subject.
It would also contain a chat-bot that is implemented with a RAG structure.
THe LLM used will be llama3.2:8B (locally downloaded to my machine)
The database (PostgreDB) will also be used via Docker
